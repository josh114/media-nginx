version: "3.8"

services:
  media-nginx:
    image: nginx:1.25-alpine
    container_name: media-nginx
    restart: unless-stopped

    volumes:
      # 🔑 Shared Coolify-managed volume (READ-ONLY)
      - h0k8884ook0gkcg4ggs4o8ko-media_storage:/var/media:ro

      # Nginx config
      - ./nginx.conf:/etc/nginx/conf.d/default.conf:ro

    expose:
      - "8080"

    networks:
      - coolify

volumes:
  # 🚨 IMPORTANT: Must be external, already created by Coolify
  h0k8884ook0gkcg4ggs4o8ko-media_storage:
    external: true

networks:
  coolify:
    external: true
